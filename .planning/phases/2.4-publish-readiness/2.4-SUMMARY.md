# Phase 2.4 完成总结

## 完成日期

2026-07-02

## 交付物

- `README.md`：修正部署说明，明确 MVP 推荐纯静态发布，Express 仅作为本地预览/自托管入口。
- `README_EN.md`：从旧的 American English IPA 工具说明更新为 PhoneticMaster 多语言纯前端训练器说明。
- `PRD.md`：从旧单语言 PRD 更新为当前 M2 能力和 M3/M4 路线的产品需求文档。
- `index.html`：页面标题更新为 PhoneticMaster 多语言训练器。
- `.planning/phases/2.4-publish-readiness/2.4-QA.md`：新增发布前自动检查、静态发布检查和浏览器冒烟清单。
- `.planning/STATE.md`、`.planning/ROADMAP.md`：M2 标记为完成，当前阶段切换到 M3 Phase 3.1。

## 验证

```bash
GITHUB_PAGES=true npm run build
curl -I -L https://ichthyoplanktonzyh.github.io/PhoneticMaster/
curl -L https://ichthyoplanktonzyh.github.io/PhoneticMaster/ | sed -n '1,40p'
```

结果：

- GitHub Pages 构建通过。
- `dist/index.html` 中静态资源引用以 `/PhoneticMaster/assets/` 开头。
- 公开 Pages 首页返回 HTTP 200。
- 公开 Pages HTML 可正常返回，并引用 `/PhoneticMaster/assets/*`。

## 偏离计划

- Phase 2.4 没有新增产品功能，集中处理发布叙事、静态构建和 QA 清单。
- 完整浏览器手动 QA 仍作为发布前清单保留，当前阶段完成的是可重复检查入口和公开页面可达性确认。

## 遗留问题

- `README_EN.md` 仍保持精简版，没有逐项翻译所有中文文档细节。
- Vite chunk-size warning 仍存在，可在后续做 code splitting。
- Web Speech API 的发音质量仍不可控，Phase 3.1 会为最小对立体预留标准音频字段。

## 经验

- M2 的核心发布前风险不是构建本身，而是外部文档仍残留旧的单语言定位。
- GitHub Pages 部署必须同时验证 `GITHUB_PAGES=true` 构建产物和公开 URL，否则容易出现 base path 正确但线上配置未启用的问题。
