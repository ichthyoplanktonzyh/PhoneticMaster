# Phase 2.1 完成总结

## 完成日期

2026-07-02

## 交付物

- `src/components/OnboardingView.tsx` — L2 成为唯一必选项；L1 改为可选、可取消，并阻止 L1 与 L2 相同。
- `src/App.tsx` — 支持无 L1 直接进入训练；语言偏好只要求保存 L2；无 L1 时隐藏推荐层并显示“直接训练”状态。
- `src/App.tsx` — 顶部训练设置支持目标语言、训练类型、难度、主题和词数，设置变化会重开当前题组。
- `src/utils/trainingSession.ts` — 新增训练会话工具，集中处理抽题、音素筛选和 fresh session 初始化。
- `src/components/TrainingView.tsx` — 看词听音模式增加显式 Previous/Next 导航。

## 偏离计划

- 原计划影响 `src/components/PhoneticKeypad.tsx`、`src/utils/phonemeGroups.ts`、`src/types.ts`，实际无需修改这些文件即可完成本 phase。
- 新增 `src/utils/trainingSession.ts`，用于承接原本计划中的“抽离训练设置与抽题逻辑”。
- 仍保留训练结束时的 `alert()`，完整结果页进入 Phase 2.2。

## 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过；Vite 仍提示 chunk size warning，但不影响构建。
- 浏览器手动验证：
  - 首次入口只选择 L2、不选择 L1，可以进入训练。
  - 无 L1 时 header 显示“直接训练 → English/中文”，SmartRecommend 不出现。
  - 英语 IPA 拼写入口可用。
  - 中文 Pinyin 拼写入口可用，显示拼音输入框、声母/韵母/声调键盘。
  - 看词听音模式 Previous/Next 导航可用。
  - 难度、Topic、Words 设置变化会重开题组。

## 遗留问题

- Phase 2.2 继续处理 nearMatch 独立状态、diff 展示、结果页和最近训练记录。
- 训练完成仍使用 `alert()`，等待 Phase 2.2 替换。
- `src/components/IPAKeypad.tsx` 旧组件、英语 `WordData` legacy 格式等既有技术债未在本 phase 处理。

## 经验

- L1-aware 推荐应只作为可选侧栏存在；训练主流程只依赖 L2 和 LanguageProfile。
- 会话初始化集中到工具函数后，难度、主题、词数、模式切换都能复用同一套 reset 行为。
