# This project requires no API keys or external services.
# - Word bank is local (src/data/wordBank.ts)
# - Pronunciation uses browser Web Speech API
# No configuration needed — just npm install && npm run dev
